import 'package:flutter/material.dart';

class AppWidgets {
  static AppBar appBar(List<Widget>? actions, Widget? leading) => AppBar(
        backgroundColor: Colors.transparent,
        actions: actions,
        leading: leading,
      );

      
}
